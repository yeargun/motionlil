import { ValueType, valueTypeRecord } from "./types";

export ValueType autoType = valueTypeRecord(object{test: (JsValue v) => {
    if (v is string) {
      return v == "auto";
    }
    return false;
  }, parse: (JsValue v) => v});
