import { numberType } from "./numbers/index";
import { ValueType, valueTypeRecord } from "./types";

export ValueType intType = valueTypeRecord(object{test: numberType.test, parse: numberType.parse, transform: (JsValue v) => {
    if (v is float) {
      return v.round();
    }
    return 0.0;
  }});
